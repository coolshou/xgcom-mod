1.project home:
	https://github.com/coolshou/xgcom-mod

2.get source by git:
	git clone https://github.com/coolshou/xgcom-mod.git xgcom

3.library needed while compiling:
	make, automake，libglib2.0-dev，libvte-dev，libgtk2.0-dev

4.compile steps:
	xgcom$ ./autogen.sh
	xgcom$ make
	xgcom$ sudo make install
	xgcom$ xgcom

TODO

    gtk3 port
    font config
    vte buffer size configable
    ascii table dialog improve
    vte buffer/screen clean/select all/copy/past
    window location save/restore
    micro script interact with vte
    right mouse menu: copy/paste
    multi tab ( do we need this) 
